find_program(CUDAToolkit_NVCC_EXECUTABLE ${CMAKE_CXX_COMPILER} ${SYCL_BIN_DIR})

find_program(CUDAToolkit_NVCC_other_EXE nvcc_other)

find_program(CUDAToolkit_NVCC_EXECUTABLE NAMES ${CMAKE_CXX_COMPILER} ${CMAKE_CXX_COMPILER}.exe
    PATHS ${SYCL_BIN_DIR}
    NO_DEFAULT_PATH
    )

find_program(BIN2C bin2c)
