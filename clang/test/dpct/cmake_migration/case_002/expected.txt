project(foo-bar LANGUAGES CXX )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsycl")
include(dpct.cmake)

project(foo2  CXX)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsycl")
include(dpct.cmake)

project(foo VERSION 1.7
DESCRIPTION "cuda test"
LANGUAGES CXX 
)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsycl")
include(dpct.cmake)

project(foo VERSION 1.1.0
LANGUAGES)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsycl")
include(dpct.cmake)

project(foo)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsycl")
include(dpct.cmake)

project( tiny-cuda-nn
	VERSION 1.7
	DESCRIPTION "Lightning fast & tiny C++/ neural network framework"
	LANGUAGES CXX 
)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsycl")
include(dpct.cmake)
