set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -m64")

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -m32")

set(SYCL_64_BIT_DEVICE_CODE "${${CMAKE_CXX_FLAGS}:MATCHES "-m64"}")
if(${SYCL_64_BIT_DEVICE_CODE})

set(SYCL_64_BIT_DEVICE_CODE "${${CMAKE_CXX_FLAGS}:MATCHES "-m64"}")
if(${SYCL_64_BIT_DEVICE_CODE} AND X)

set(SYCL_64_BIT_DEVICE_CODE "${${CMAKE_CXX_FLAGS}:MATCHES "-m64"}")
if(X AND ${SYCL_64_BIT_DEVICE_CODE})

set(SYCL_64_BIT_DEVICE_CODE "${${CMAKE_CXX_FLAGS}:MATCHES "-m64"}")
if(X OR ${SYCL_64_BIT_DEVICE_CODE} OR Y)
